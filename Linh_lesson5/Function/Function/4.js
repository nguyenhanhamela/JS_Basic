// (khó) viết hàm kiểm tra một chuỗi có chứa chuỗi con hay không, hàm này nhận tham số là 1 chuỗi cha và 1 chuỗi con, 
// trả lại true nếu chuỗi con có xuất hiện trong chuỗi cha, nếu ngược lại thì trả lại false. (không được dùng các hàm include của string) 
// ví dụ: chuỗi cha là "abcdef" , chuỗi con là "bcd" => thì có nghĩa là chuỗi cha có chứa chuỗi con 
// Let  